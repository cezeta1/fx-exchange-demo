// Angular
import { Injectable } from '@angular/core';
// Rxjs
// import { of, Observable, map, tap, BehaviorSubject, zip, skip } from 'rxjs';

@Injectable({
  providedIn: 'root',
})
export class AuthService {
  constructor() {}
}
